import { Router } from 'express';

const verificationRouter = Router();

verificationRouter
  .get('/:id', (req, res, next) => {
  })
  .post('/', (req, res, next) => {
  });

export default verificationRouter;